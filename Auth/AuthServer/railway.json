{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Auth/AuthServer/Dockerfile.backend"
  },
  "deploy": {
    "startCommand": "dotnet AuthServer.dll",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
